import 'package:flutter/material.dart';

class ButtomsScreen extends StatelessWidget {
  const ButtomsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Buttoms screen"),
      ),
      body: const Placeholder(),
    );
  }
}
